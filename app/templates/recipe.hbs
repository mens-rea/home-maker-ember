<div class="row pb-2">
  <h2>{{@model.title}}</h2>
  <p class="w-100">{{{@model.description}}}</p>
</div>

<SubItemList @recipeModel={{this.model}} @ingredients={{this.ingredients}} @type="ingredient" />
