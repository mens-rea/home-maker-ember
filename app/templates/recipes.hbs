<h2>Recipes</h2>

<div class="row">
  {{#each @model as |recipe|}}
    <div class="col-md-4 mb-3">
      <Card @title={{recipe.title}} @description={{recipe.description}}/>
    </div>
  {{/each}}
</div>

{{outlet}}

<footer class="text-right">Created by JM Alvarez @2020</footer>